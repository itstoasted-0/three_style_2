<% provide(:title, '投稿一覧') %>
<h1>投稿一覧</h1>
<% if user_signed_in? %>
  <% @posts.each do |post| %>
    <p><%= post.genre %> : <%= post.content %> : <%= post.user.name.present? ? "投稿者 #{post.user.name}" : "会員No.#{post.user.id}" %></p>
  <% end %>
<% else %>
  <p>ログインしてください</p>
<% end %>